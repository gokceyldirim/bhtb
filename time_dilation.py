"""The function calculates and returns the dilated time by using Schwarzschild Equation.G = gravitational constant=(6.674 × 10⁻¹¹ m³/kg/s²)c = speed of light=(299,792,458 m/s)"""import mathG = 6.67430e-11   # gravitational constantc = 299792458.0   # speed of lightdef calculate_time_dilation(mass, distance, original_time):    factor = 1 - (2 * G * mass) / (distance * c**2)    if factor < 0:        r_s = (2 * G * mass) / c**2        raise ValueError(            f"distance ({distance:.2f} m) must exceed Schwarzschild radius {r_s:.2f} m"        )    return original_time * math.sqrt(factor)